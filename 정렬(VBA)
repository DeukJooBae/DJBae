Sub 정렬()
  k = 0
    for i =1  to 9
       treaty_num = cells(2,i).value
       
       for j = 0 to treaty_num - 1
          range("N"& k + 3).value = cells(3+j,i).value
          
          k=k+1
        next
     next
 end sub
